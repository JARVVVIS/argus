from setuptools import setup, find_packages

setup(
    name="video_models",
    version="0.1.0",
    packages=find_packages(),  # Automatically find all packages and subpackages
    description="A collection of video model modules that we can run to generate captions",
    author="Ruchit Rawal",
)
